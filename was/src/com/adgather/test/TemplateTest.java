package com.adgather.test;



public class TemplateTest {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		/*
		// TODO Auto-generated method stub
		String charset="utf-8", dir="/work/enliple/www/webapp/public_html/test/", file="banner.ftl", content="";
		Map map = new HashMap<String, String>();
		map.put("info_json", "bb");
		map.put("img_json", "aa");
		
		try	{
			Configuration cfg = new Configuration();

			cfg.setDirectoryForTemplateLoading(new File(dir));
			cfg.setObjectWrapper(new DefaultObjectWrapper());
			cfg.setDefaultEncoding(charset);
			cfg.setEncoding(Locale.KOREAN, charset);

			Template temp = cfg.getTemplate(file);
			ByteArrayOutputStream os = new ByteArrayOutputStream();
			Writer output = new OutputStreamWriter(os, charset);

			try {
				temp.process(map, output);
			} catch (TemplateException e) {
				e.printStackTrace();
			}

			output.flush();
			output.close();
			os.close();
			map.clear();

			content = os.toString(charset);
			System.out.println( content );
		}catch (Exception e) {
		}
		*/
	}

}
